import React from 'react'

const JSXRules = () => {
  return (
    <div>
        <h1> JSX Rules</h1>
        <p>JSX must return a <strong>single parent element.</strong></p>
        <p>JSX elements must be <strong>properly closed</strong></p>
        <p>JSX attributes are written using <strong>camelCase (e.g., className instead of class.)</strong></p>
    </div>
  )
}

export default JSXRules